import { useEffect, useRef, useState } from 'react'; import { StyleSheet, Text, View } from 'react-native'; import { Magnetometer } from 'expo-sensors';
const directions = ['N', 'NE', 'E', 'SE', 'S', 'SW', 'W', 'NW'];
export default function CompassScreen() { const [heading, setHeading] = useState(0); const [message, setMessage] = useState('Keep phone flat for best results'); const smoothed = useRef(0); useEffect(() => { let sub; (async () => { if (!await Magnetometer.isAvailableAsync()) { setMessage('Compass sensor is not available on this device.'); return; } Magnetometer.setUpdateInterval(200); sub = Magnetometer.addListener(({ x, y, z }) => { const strength = Math.sqrt(x*x+y*y+z*z); let target = (Math.atan2(y, x) * 180 / Math.PI + 360) % 360; let delta = ((target - smoothed.current + 540) % 360) - 180; smoothed.current = (smoothed.current + delta * .18 + 360) % 360; setHeading(Math.round(smoothed.current)); setMessage(strength < 20 || strength > 70 ? 'Calibration needed: move your phone in a figure-eight.' : 'Compass active. Keep away from metal objects.'); }); })(); return () => sub?.remove(); }, []); return <View style={styles.page}><Text style={styles.heading}>{heading}°</Text><Text style={styles.direction}>{directions[Math.round(heading / 45) % 8]}</Text><View style={styles.compass}><Text style={styles.north}>N</Text><Text style={styles.east}>E</Text><Text style={styles.south}>S</Text><Text style={styles.west}>W</Text><View style={[styles.needle, { transform: [{ rotate: `${heading}deg` }] }]}><View style={styles.red} /><View style={styles.white} /></View><View style={styles.center} /></View><Text style={styles.note}>{message}</Text><Text style={styles.help}>Compass readings may be affected by phone cases, metal objects, and poor calibration.</Text></View>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: '#0B1118', alignItems: 'center', padding: 28 }, heading: { color: '#F5F9FF', fontSize: 58, fontWeight: '900', marginTop: 28 }, direction: { color: '#57E389', fontSize: 20, fontWeight: '800' }, compass: { width: 290, height: 290, borderRadius: 145, borderWidth: 2, borderColor: '#2D4055', marginTop: 50, justifyContent: 'center', alignItems: 'center' }, north: { position: 'absolute', top: 16, color: '#FF5C70', fontWeight: '900', fontSize: 22 }, east: { position: 'absolute', right: 18, color: '#B7C7D9', fontWeight: '800' }, south: { position: 'absolute', bottom: 18, color: '#B7C7D9', fontWeight: '800' }, west: { position: 'absolute', left: 18, color: '#B7C7D9', fontWeight: '800' }, needle: { width: 24, height: 210, alignItems: 'center', justifyContent: 'center' }, red: { height: 105, width: 6, backgroundColor: '#FF5C70', borderTopLeftRadius: 4, borderTopRightRadius: 4 }, white: { height: 105, width: 6, backgroundColor: '#DCEBFA' }, center: { position: 'absolute', width: 18, height: 18, borderRadius: 9, backgroundColor: '#2AABEE' }, note: { color: '#FFB020', textAlign: 'center', marginTop: 35, fontWeight: '700' }, help: { color: '#8293A8', textAlign: 'center', lineHeight: 20, marginTop: 16 } });
